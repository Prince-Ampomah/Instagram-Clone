import 'package:flutter/material.dart';

import '../../../../core/constants/constants.dart';

class PostedItem extends StatelessWidget {
  const PostedItem({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(Const.princeImage);
  }
}
